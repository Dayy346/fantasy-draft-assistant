{
  "name": "fantasy-draft-assistant",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "etl": "npm run etl --prefix backend"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}